/** @param {NS} ns */
export async function main(ns) {

    // buy servers up to desired number (set in ./data/cfg.data)

    

    // add to array each server to be upgraded

        // loop
    // prune for servers that have reached the RAM requirement (set in ./data/cfg.data)

    // find cheapest upgrade


}